<div class="row text-center">
  <h3>{{title}}</h3>
  {% for item in items %}
  <div class="col-md-5ths col-sm-4 col-xs-6">
     <img class="image-crop" src="{{item}}" alt = "">
  </div>
  {% endfor %}
</div>
